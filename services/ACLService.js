const User = require('../models/User');

class ACLService {
    constructor() {

    }

    /**
     * @param {User} user
     * @param {string[]} roles
     * @returns {boolean}
     */
    checkRole(user, roles) {

    }
}

module.exports = new ACLService();
